# dotfiles
A repository for storing dotfiles.

#ToDo
- Add color suport to zsh
- Add unicode support to tmux (for now alias tmux=tmux -u).
- configure starship and add more prompts (only one can be used at a time).
